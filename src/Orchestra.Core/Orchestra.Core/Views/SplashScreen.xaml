<catel:DataWindow x:Class="Orchestra.Views.SplashScreen"
                  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                  xmlns:catel="http://catel.codeplex.com"
                  xmlns:controls="clr-namespace:Orchestra.Controls"
                  WindowStartupLocation="CenterScreen" WindowStyle="None" Topmost="true" 
                  SizeToContent="WidthAndHeight" Background="{x:Null}" ResizeMode="NoResize" AllowsTransparency="True">

    <Grid Width="450" Height="250">
        <Grid.RowDefinitions>
            <RowDefinition Height="25" />
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Row="1" Grid.ColumnSpan="2" Text="{Binding Title}" HorizontalAlignment="Center" FontSize="48" Foreground="White" />

        <controls:BusyIndicator Grid.Row="2" Grid.ColumnSpan="2" Foreground="White" />

        <TextBlock Grid.Row="3" Grid.Column="0" Text="Starting..." Foreground="White" Margin="6" HorizontalAlignment="Left"/>
        <TextBlock Grid.Row="3" Grid.Column="1" Margin="6" HorizontalAlignment="Right">
            <Hyperlink Command="{Binding OpenUrl}" TextDecorations="None">
                <TextBlock Text="{Binding Company, StringFormat=Powered by {0}}" Foreground="White" />
            </Hyperlink>
        </TextBlock>

    </Grid>

</catel:DataWindow>

<orchestra:DocumentView x:Class="Orchestra.Modules.OxyPlot.Views.OxyPlotView"
                        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
				        xmlns:catel="http://catel.codeplex.com"
                        xmlns:oxy="clr-namespace:OxyPlot.Wpf;assembly=OxyPlot.Wpf"
                        xmlns:orchestra="http://orchestra.codeplex.com">

    <catel:StackGrid>
		<catel:StackGrid.RowDefinitions>
			<RowDefinition Height="*" />
			<RowDefinition Height="*" />
		</catel:StackGrid.RowDefinitions>

        <catel:StackGrid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
        </catel:StackGrid.ColumnDefinitions>

        <oxy:Plot Model="{Binding Model1}" />
        <oxy:Plot Model="{Binding Model2}" />
        <oxy:Plot Model="{Binding Model3}" />
        <oxy:Plot Model="{Binding Model4}" />
    </catel:StackGrid>
    
</orchestra:DocumentView>

What steps to perform when releasing:

1) Release via NuGet
2) Release via Codeplex
3) Update the following version numbers:
   * Version numbers in final builder
   * History.txt
   * SolutionAssemblyInfo.cs

- Optional
1) Share via Twitter
2) Share via LinkedIn
